# ============================================================================
#                           === CONFIGURATION FILE ===
# ============================================================================
#                           --- EXTRACT SETTINGS ---
#               Options for the extract part of the pipeline.
# ----------------------------------------------------------------------------
# PATH TO SOCIAL SOUL INPUT XML EXAMPLE
#   Path to example XML file generated by Social Soul containing offer data.
#   For testing purposes only.
PATH_TO_EXAMPLE_SOCIAL_SOUL_INPUT_XML_FILE = './rsc/xml/LomadeeDownload.xml'
#
#
# ----------------------------------------------------------------------------
#                           --- TRANSFORM SETTINGS ---
#               Options for the transform part of the pipeline.
# ----------------------------------------------------------------------------
# DEFAULT PATH TO TEMPORARY PARQUET FOLDER
#   During execution, the program may generate intermediary, temporary parquet
#   files to transport data between scripts. Such files will be temporarily 
#   stored in this folder.
DEFAULT_PATH_TO_TMP_PARQUET_FOLDER = './tmp/parquet/'

IG_READY_DF_FILE_NAME = 'offers_df_ig_ready'

DEFAULT_PATH_TO_STORY_TEMPLATE = './rsc/img/tmpl_story_720x1280_final.png'

DEFAULT_PATH_TO_TMP_IMAGE_FOLDER = './tmp/img/'

COLUMNS_OF_INTEREST_LIST = [
    'offerName',            # Offer description
    #'sellerId',
    #'sellerThumbnail',
    'offerLink',            # URL to offer
    'offerThumbnail',       # Photo of product in white background (most of the time)
    'priceFrom',            # Price without discount (price before)
    #'sellerName',
    'priceTo',              # Price with discount (price after)
    #'sku',
    #'categoryName',
    #'categoryId',
]

#
#
# ----------------------------------------------------------------------------
#                           --- LOAD SETTINGS ---
#               Options for the load (post) part of the pipeline.
# ----------------------------------------------------------------------------
# DEFAULT DISPLAY TEXT FOR IG LINK STICKER:
# This is the default text to be displayed on IG's link sticker when posting 
# offers as IG stories.
DEFAULT_LINK_STICKER_TXT = 'ver oferta'